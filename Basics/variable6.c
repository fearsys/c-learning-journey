#include <stdio.h>
#include <stdbool.h>

int main () {
    bool isOnline = true;
    printf("%d\n", isOnline); // %d shows a digit . true = 1 , false = 0;
    bool isStudent = false;
    printf("%d\n", isStudent);
    
    return isStudent; // Seems like I can also do this . cool !!

}